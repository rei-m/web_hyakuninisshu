import { startTraining } from '../../src/actions/trainings';

describe('TrainingActionCreator', () => {
  it('should create startTraining Action', () => {
    return expect('hoge').toEqual('hoge');
  });
});
